from django.shortcuts import render
"""
from django.shortcuts import render
from django.contrib import messages
from django.http import HttpResponseRedirect
from django.urls import reverse
from .logic import acompanante_logic as vl

# Create your views here.
 def acompanante_view(request, pk):
     if request.method == 'GET':
        acompanante = vl.get_acompanante(pk)
        acompanante_dto = serializers.serialize('json', 'acompanante')
        return HTTPResponse(acompanante_dto, 'application/json')

"""